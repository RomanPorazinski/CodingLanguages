For loop allows for executing highly repetitive actions with predictable change of inputs/variables

```c
for (int i = 0; i < 5; i++) {
    printf("i = %d\n", i); // C
    // std::cout << "i = " << i << std::endl; // C++
}
```